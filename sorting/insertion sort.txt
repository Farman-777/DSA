//iterative
#include <iostream>
using namespace std;

int main()
{
int i,j,temp;
int a[]={-1,0,27,23,20,25,16,18,15};
int n=sizeof(a)/sizeof(a[0]);
for(i=1;i<n;i++)
{
    temp=a[i];
    for(j=i-1;j>=0;j--)
    {
if(a[j]>a[j+1])
swap(a[j],a[j+1])
else
break;
       
    }
  
}

for(i=0;i<n;i++)
cout<<a[i]<<" ";
    return 0;
}

//recursive

#include <iostream>
using namespace std;

void insertionSort(int a[],int i,int n)
{  
    int j;
    cout<<"hi"<<endl;
    if(n==0)
    return;

    for(j=i-1;j>=0;j--)
    {
     if(a[j]>a[j+1])
     swap(a[j],a[j+1]);
     else
     break;
       
    }
    i++;
    insertionSort(a,i,n-1);
  
}

   
    

int main()
{
    int i;
    int a[]={50,40,26,27,28};
    int n=sizeof(a)/sizeof(a[0]);
    insertionSort(a,0,n);
    for(i=0;i<n;i++)
    cout<<a[i]<<" ";

    return 0;
}