
#include <iostream>
using namespace std;

void bubbleSort(int arr[],int n)
{  
  int j;
  if(n==0 || n==1)
  return;
    for(j=0;j<n;j++)
    {
        if(arr[j]>arr[j+1])
        swap(arr[j],arr[j+1]);
    }
    bubbleSort(arr,n-1);
}
    

int main()
{
    int i;
    int arr[]={50,40,26,27,28};
    int n=sizeof(arr)/sizeof(arr[0]);
    bubbleSort(arr,n);
    for(i=0;i<n;i++)
    cout<<arr[i]<<" ";

    return 0;
}